{% extends "forms/field.html.twig" %}

{% block input %}
    <div class="{{ form_field_wrapper_classes ?: 'mb-2' }} {{ field.size }} {{ field.wrapper_classes }}">
        <input
            {# required attribute structures #}
            name="{{ (scope ~ field.name)|fieldName }}"
            value="{{ value|join(', ')|e('html_attr') }}"
            {# input attribute structures #}
            {% block input_attributes %}
                class="block w-full px-8 py-5 bg-white/40 backdrop-blur-3xl border border-brand-primary/10 rounded-[1.5rem] shadow-inner-lg placeholder-brand-dark/20 focus:outline-none focus:ring-8 focus:ring-brand-primary/5 focus:border-brand-primary/40 focus:bg-white/80 text-brand-dark font-black text-xl transition-all duration-500 hover:border-brand-primary/30 {{ field.classes }}"
                {% if field.id is defined %}id="{{ field.id|e }}" {% endif %}
                {% if field.style is defined %}style="{{ field.style|e }}" {% endif %}
                {% if field.disabled or isDisabledToggleable %}disabled="disabled"{% endif %}
                {% if field.placeholder %}placeholder="{{ field.placeholder|t }}"{% endif %}
                {% if field.autofocus in ['on', 'true', 1] %}autofocus="autofocus"{% endif %}
                {% if field.novalidate in ['on', 'true', 1] %}novalidate="novalidate"{% endif %}
                {% if field.readonly in ['on', 'true', 1] %}readonly="readonly"{% endif %}
                {% if field.autocomplete in ['on', 'off'] %}autocomplete="{{ field.autocomplete }}"{% endif %}
                {% if field.attributes is defined %}
                  {% for attribute in field.attributes %}
                      {{ attribute.name }}="{{ attribute.value }}"
                  {% endfor %}
                {% endif %}
                {% if required %}required="required" aria-required="true"{% endif %}
                {% if field.validate.pattern %}pattern="{{ field.validate.pattern|e }}"{% endif %}
                {% if field.validate.min is defined %}min="{{ field.validate.min }}"{% endif %}
                {% if field.validate.max is defined %}max="{{ field.validate.max }}"{% endif %}
                {% if field.validate.step is defined %}step="{{ field.validate.step }}"{% endif %}
                {% if field.help %}aria-describedby="help-{{ field.name|e }}"{% endif %}
            {% endblock %}
            type="{{ field.type|default('text') }}"
            />
    </div>
{% endblock %}
