<div class="inline-flex items-center gap-3 px-6 py-2.5 bg-surface-sunken/60 backdrop-blur-xl border border-brand-primary/10 rounded-2xl shadow-xl shadow-black/5">
    <span class="la la-calendar-alt text-xl text-brand-primary" aria-hidden="true"></span>
    <time class="dt-published text-[10px] font-black uppercase tracking-[0.2em] text-brand-dark/60" datetime="{{ page.date|date("c") }}">
        {{ page.date|date(system.pages.dateformat.short) }}
    </time>
</div>
