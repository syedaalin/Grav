{
  "name": "grav-theme-nur-ul-huda",
  "version": "2.2.0",
  "description": "Nur-ul-Huda Theme for Grav CMS - Modern Islamic theme with Pure Custom CSS (Modern Vanilla), prayer times, Hijri calendar, and LMS integration",
  "main": "index.php",
  "license": "MIT",
  "scripts": {
    "dev": "npx lightningcss --bundle --targets '>= 0.25%' ./css/modern-vanilla.css -o ./css/nur-ul-huda.css && echo 'CSS bundled for development'",
    "build": "npx lightningcss --minify --bundle --targets '>= 0.25%' ./css/modern-vanilla.css -o ./css/nur-ul-huda.css",
    "watch": "npx chokidar 'css/**/*.css' -c 'npm run dev'"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "lightningcss-cli": "^1.21.0"
  }
}
