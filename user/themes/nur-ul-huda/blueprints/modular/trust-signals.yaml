title: Trust Signals
"@extends": default

form:
  fields:
    tabs:
      fields:
        trust:
          type: tab
          title: Trust Signals
          fields:
            header.signals:
              type: list
              label: Trust Signals
              fields:
                .title:
                  type: text
                  label: Statistic/Title (e.g. 10k+)
                .text:
                  type: text
                  label: Label (e.g. Students)
                .icon:
                  type: iconpicker
                  label: Icon
                .color:
                  type: colorpicker
                  label: Background Color
