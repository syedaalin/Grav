<footer class="aabtaab-footer">
    <div class="footer-container">
        
        <div class="footer-col">
            <h3 class="footer-heading">AabTaab</h3>
            <p class="footer-tagline">Faith. Knowledge. Access.</p>
            <div class="social-section">
                <p><strong>Social Media</strong></p>
                <div class="social-icons">
                    <a href="#" target="_blank" class="social-btn"><i class="fa fa-facebook"></i> Facebook</a>
                    <a href="#" target="_blank" class="social-btn"><i class="fa fa-instagram"></i> Instagram</a>
                    <a href="#" target="_blank" class="social-btn"><i class="fa fa-youtube"></i> YouTube</a>
                </div>
            </div>
        </div>

        <div class="footer-col">
            <h3 class="footer-heading">Contact Us</h3>
            <ul class="contact-list">
                <li>
                    <span class="icon">📍</span> 
                    <span>R-591 Block 20 F B Area Karachi.</span>
                </li>
                <li>
                    <span class="icon">✉️</span> 
                    <a href="mailto:edu.aabtaab@gmail.com">edu.aabtaab@gmail.com</a>
                </li>
                <li>
                    <span class="icon">📞</span> 
                    <a href="tel:+923022452000">+92 302 245 2000</a>
                </li>
            </ul>
        </div>

        <div class="footer-col">
            <h3 class="footer-heading">Newsletter</h3>
            <p>Subscribe to get the latest updates.</p>
            <form class="footer-form" action="#">
                <input type="email" placeholder="Your Email Address" required>
                <button type="submit">Subscribe</button>
            </form>
            <div class="donate-wrapper">
                <a href="#" class="donate-btn">Donate Now</a>
            </div>
        </div>

    </div>

    <div class="footer-bottom">
        <div class="bottom-content">
            <p>Developed By: <strong>Aabtaab Team</strong></p>
            <p>&copy; {{ "now"|date("Y") }} <strong>{{ site.title }}</strong>. All rights reserved.</p>
            <p class="sustainability-index">🍃 <span title="Estimated CO2 per page view">Sustainability: {{ theme_config.sustainability_score|default('A+') }} (approx. {{ theme_config.co2_per_view|default('0.2g') }} CO₂/view)</span></p>
        </div>
    </div>
</footer>

<style>
    /* Footer styles inherited and optimized for my-quark token system */
    .aabtaab-footer {
        background: var(--color-footer-bg, oklch(0.11 0 0));
        color: var(--color-footer-text, #fff);
        padding: 48px 0 0 0;
        font-family: var(--font-body, inherit);
        font-size: 15px;
        line-height: 1.5;
    }

    .footer-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: var(--space-md, 24px);
        max-width: var(--site-width, 1200px);
        margin: 0 auto;
        padding: 0 20px 36px;
        align-items: start;
    }

    .footer-col { padding: 6px 0 }

    .footer-heading {
        color: var(--color-footer-text, #fff);
        font-size: 18px;
        margin-bottom: 14px;
        font-weight: 700;
        text-transform: uppercase;
        border-bottom: 2px solid var(--color-footer-accent, #d4af37);
        display: inline-block;
        padding-bottom: 6px;
    }

    .footer-tagline { 
        color: var(--text-muted);
        font-style: italic;
        margin-bottom: 12px;
    }

    .aabtaab-footer a {
        color: inherit;
        text-decoration: none;
        transition: color 0.18s, transform 0.15s;
    }

    .aabtaab-footer a:hover {
        color: var(--color-footer-accent, #d4af37);
    }

    .social-icons {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
    }

    .social-btn {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: rgba(255,255,255,0.05);
        padding: 10px 12px;
        border-radius: var(--radius-tight, 6px);
        font-size: 14px;
    }

    .social-btn:hover {
        background: var(--color-footer-accent, #d4af37);
        color: #000;
    }

    .contact-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .contact-list li {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 12px;
    }

    .footer-form {
        display: flex;
        gap: 0;
        margin-top: 8px;
        margin-bottom: 18px;
    }

    .footer-form input {
        padding: 12px 14px;
        border: 0;
        border-radius: var(--radius-tight, 6px) 0 0 var(--radius-tight, 6px);
        flex: 1;
        background: #fff;
        color: #111;
        font-size: 14px;
    }

    .footer-form button {
        background: var(--color-footer-accent, #d4af37);
        color: #000;
        border: 0;
        padding: 12px 16px;
        border-radius: 0 var(--radius-tight, 6px) var(--radius-tight, 6px) 0;
        font-weight: 700;
        cursor: pointer;
    }

    .donate-btn {
        background: #b91c1c;
        color: #fff;
        padding: 12px 20px;
        border-radius: var(--radius-soft, 12px);
        display: inline-block;
        font-weight: 700;
        transition: transform 0.12s;
    }

    .donate-btn:hover {
        transform: translateY(-3px);
    }

    .footer-bottom {
        background: rgba(0,0,0,0.3);
        padding: 14px 0;
        font-size: 13px;
        color: var(--text-muted);
        border-top: 1px solid rgba(255,255,255,0.05);
    }

    .bottom-content {
        max-width: var(--site-width, 1200px);
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        padding: 0 20px;
        flex-wrap: wrap;
    }

    @media (max-width: 768px) {
        .footer-container { grid-template-columns: 1fr; }
        .bottom-content { flex-direction: column; text-align: center; }
    }
</style>
