{% set logo = theme_var('agent_identity_logo') %}
{% set brand = theme_var('agent_identity_brandName')|default(site.title) %}
{% set show_text = theme_var('agent_identity_showText') %}

<a href="{{ home_url }}" class="navbar-brand mr-10" style="display: flex; align-items: center; gap: 12px;">
    {% if logo %}
      {% set logo_file = (logo|first).name %}
      <img src="{{ url('theme://images/logo/' ~ logo_file)  }}" alt="{{ brand }}" fetchpriority="high" />
    {% endif %}

    {% if show_text or not logo %}
      <span class="brand-text">{{ brand }}</span>
    {% endif %}
</a>